#> arcane_core:api/entity/kill_recursive
#
# Kill the executing entity and all of it's passengers at the end of it's tick code execution.
#
# @api
# @context entity

tag @s add ac.kill
tag @s add ac.kill.recursive
